import { DateSecondAddPipe } from './date-second-add.pipe';

describe('DateSecondAddPipe', () => {
  it('create an instance', () => {
    const pipe = new DateSecondAddPipe();
    expect(pipe).toBeTruthy();
  });
});
